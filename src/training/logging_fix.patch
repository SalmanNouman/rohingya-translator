diff --git a/src/train.py b/src/train.py
index abc123..def456 789
--- a/src/train.py
+++ b/src/train.py
@@ -100,7 +100,7 @@ def validate(model, val_dataloader, tokenizer, device):
     model.eval()
     total_loss = 0
     
-    logger.error("") # Remove this line
+    logger.info("Starting validation...") # Change to info level
     
     with torch.no_grad():
         for batch in tqdm(val_dataloader, desc="Validation", leave=True):
@@ -115,7 +115,7 @@ def validate(model, val_dataloader, tokenizer, device):
             
             total_loss += loss.item()
     
-    logger.error(f"Validation Loss: {total_loss / len(val_dataloader)}")
+    logger.info(f"Validation Loss: {total_loss / len(val_dataloader)}")
     return total_loss / len(val_dataloader)
